@*@using BTCPayServer.Views.Stores
@model BTCPayServer.Ethereum.ViewModels.EthStoreViewModel

@{
    Layout = "../Shared/_NavLayout.cshtml";

    ViewData.SetActivePageAndTitle(StoreNavPages.ActivePage, "Ethereum Settings");

    ViewData["NavPartialName"] = "../Stores/_Nav";
}

<partial name="_StatusMessage" for="StatusMessage" />
<div class="row">
    <div class="col-md-6">
        <div asp-validation-summary="All" class="text-danger"></div>
    </div>
</div>
<div class="row">
    <div class="col-md-8">
        <div class="form-group">
            <table class="table table-sm table-responsive-md">
                <thead>
                    <tr>
                        <th>Crypto</th>
                        <th class="text-center">Enabled</th>
                        <th class="text-right">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model.EthPaymentMethodViewModels)
                    {
                        <tr>
                            <td>@item.CryptoCode</td>
                            <td class="text-center">
                                @if (item.Enabled)
                                {
                                    <span class="fa fa-check"></span>
                                }
                                else
                                {
                                    <span class="fa fa-times"></span>
                                }
                            </td>
                            <td class="text-right">
                                <a id="Modify" asp-action="GetEthPaymentMethod" asp-route-cryptoCode="@item.CryptoCode">Modify</a>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts {
    @await Html.PartialAsync("_ValidationScriptsPartial")
}*@
